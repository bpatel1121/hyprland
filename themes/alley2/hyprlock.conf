# Generated · Material You (matugen) — hyprlock
#
# TEMPLATE, not a live config. scripts/theme-apply.sh renders this to
# ~/.config/hypr/hyprlock.conf with @WALLPAPER@ substituted for the active
# theme's wallpaper. It is rendered rather than symlinked because hyprlang
# can't be relied on to expand $HOME, and this repo deliberately keeps absolute
# user paths out of tracked files (see the note about the relative `current`
# symlink in README.md). The rendered copy is gitignored.
#
# Palette discipline matches waybar: PINK is the frame, CYAN is the content,
# red appears only on failure.

general {
    hide_cursor = true
    ignore_empty_input = true
}

background {
    monitor =
    path = @WALLPAPER@
    blur_passes = 3
    blur_size = 8
    brightness = 0.55
    contrast = 1.1
}

# The clock: cyan glyphs in a pink bloom — the same pairing as the waybar clock,
# where the glow hue matches the frame rather than the text.
label {
    monitor =
    text = cmd[update:1000] date +"%H:%M"
    color = rgb(e6beae)
    font_size = 96
    font_family = JetBrainsMono Nerd Font ExtraBold
    shadow_passes = 3
    shadow_size = 8
    shadow_color = rgb(ffb596)
    shadow_boost = 1.4
    position = 0, 120
    halign = center
    valign = center
}

label {
    monitor =
    text = cmd[update:60000] date +"%A, %d %B"
    color = rgb(d8c2ba)
    font_size = 16
    font_family = JetBrainsMono Nerd Font
    position = 0, 40
    halign = center
    valign = center
}

# Terminal-noir theater under the input.
label {
    monitor =
    text = S Y S T E M   L O C K E D
    color = rgb(ffb596)
    font_size = 13
    font_family = JetBrainsMono Nerd Font
    shadow_passes = 2
    shadow_size = 5
    shadow_color = rgb(ffb596)
    shadow_boost = 0.8
    position = 0, -170
    halign = center
    valign = center
}

input-field {
    monitor =
    size = 320, 52
    outline_thickness = 2
    rounding = 12
    dots_size = 0.26
    dots_spacing = 0.3
    dots_center = true
    outer_color = rgb(ffb596)
    inner_color = rgb(271e1a)
    font_color = rgb(f1dfd9)
    check_color = rgb(e6beae)
    fail_color = rgb(ffb4ab)
    fade_on_empty = false
    # `##` is hyprlang's escape for a literal `#` — a bare one starts a comment.
    placeholder_text = <span foreground="##d8c2ba">enter password</span>
    fail_text = <span foreground="##ffb4ab">$FAIL</span>
    position = 0, -90
    halign = center
    valign = center
}
